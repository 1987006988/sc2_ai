# Evaluation

This directory owns evaluation orchestration, opponent pools, metrics, and generated reports.

It may run the bot through stable entrypoints, but bot decision logic must remain in `src/sc2bot/`.
