# Research

This directory contains prototypes and experiments only.

Research code is not production by default. `src/sc2bot/` must not import from `research/`.

Promotion to mainline requires a plan, repeatable evaluation, and a stable interface.
