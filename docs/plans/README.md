# Plans

Use plans for complex or multi-file tasks.

## Required Before

- changing multiple mainline modules;
- adding a manager;
- changing telemetry or data schema;
- adding an evaluation protocol;
- promoting a prototype;
- changing architecture boundaries.

Active plans live in `active/`. Move completed plans to `completed/`, and abandoned plans to `abandoned/`.
