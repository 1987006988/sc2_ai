# Reports (Legacy Convenience Folder)

This directory is not the primary home for project foundation materials.

Use:

- `docs/foundation/01_survey/` for survey inputs;
- `docs/foundation/02_feasibility/` for feasibility materials;
- `docs/foundation/03_direction/` for direction lock-in and phase-1 scope.

Keep this folder only for temporary compatibility or generated copies, not as the canonical location of project-defining documents.
