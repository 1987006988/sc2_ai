# Architecture Decision Records

Use ADRs for decisions that affect long-term architecture, module boundaries, dependencies, evaluation protocols, or prototype promotion.

Create a new ADR when:

- adding or removing a mainline module;
- choosing a framework;
- changing data or telemetry schema;
- promoting research code into `src/sc2bot/`;
- changing evaluation protocols;
- rejecting a major route.
