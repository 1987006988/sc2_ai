# Data

Data lifecycle:

- `raw/`: original replays or external downloads.
- `interim/`: parsed or extracted but not final data.
- `processed/`: datasets ready for training or evaluation.
- `logs/`: match, telemetry, and evaluation outputs.

Large generated data should not be committed by default.
