# Data Policy

- Commit schemas, small fixtures, and metadata.
- Do not commit large replay packs, generated datasets, or bulk logs by default.
- Every generated dataset should record source, script, config, and timestamp.
